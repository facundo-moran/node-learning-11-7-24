{
  "name": "introduccion",
  "version": "1.0.0",
  "description": "Esta es mi primera aplicación Node",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon",
    "build": "rimraf ./dist && tsc",
    "start:prod": "npm run build"
  },
  "author": "Facundo",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.10",
    "nodemon": "^3.0.1",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "winston": "^3.13.1"
  }
}
